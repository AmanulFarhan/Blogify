<nav class="navbar navbar-expand-lg bg-dark border-bottom border-body ps-5 pe-5" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand me-5" href="/">Blogify</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active me-4" aria-current="page" href="/">Home</a>
        </li>
         <% if (locals.user) { %>
            <li class="nav-item">
                <a class="nav-link active" href="/blog/add-new">Add Blog</a>
            </li>
        <% } %>
      </ul>
      <ul class="ms-auto navbar-nav">
        <% if (locals.user) { %>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle active" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <img src="<%= locals.user.profileImageUrl %>" alt="Avatar" width="30" height="30" class="rounded-circle me-2">
                        <%= locals.user.fullName %>
                </a>
                <ul class="dropdown-menu">
                    <li><a class="dropdown-item active" href="/user/logout">Logout</a></li>
                </ul>
            </li>
        <% } else { %>
            <li class="nav-item">
                <a class="nav-link active me-4" href="/user/signup">Create Account</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" href="/user/signin">Sign-In</a>
            </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>

<% if (locals.error) { %>
    <div class="alert alert-danger mt-4 container" role="alert">
        <%= locals.error %>
    </div>
<% } %>